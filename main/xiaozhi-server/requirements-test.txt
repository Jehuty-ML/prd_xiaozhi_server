# Minimal deps for gateway unit-test gate (no torch / funasr).
# Keep versions aligned with requirements.txt where overlapping.
loguru==0.7.3
prometheus_client==0.21.1
PyYAML==6.0.2
aiohttp==3.13.2
httpx==0.28.1
requests==2.34.2
numpy==1.26.4
pydub==0.25.1
opuslib_next==1.1.5
PyJWT==2.10.1
cryptography==43.0.0
