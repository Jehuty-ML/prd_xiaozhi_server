# Optional local FunASR / SenseVoice (xiaozhi-audio-receiver)
# Do not install in default CI — heavy torch stack.
# pip install -r requirements-funasr.txt
funasr>=1.2.0
torch>=2.2.0
torchaudio>=2.2.0
psutil>=5.9.0
modelscope>=1.20.0
numpy>=1.26.0
